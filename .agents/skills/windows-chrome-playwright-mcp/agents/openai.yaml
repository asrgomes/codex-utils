interface:
  display_name: "Windows Chrome Playwright MCP"
  short_description: "Control Windows Chrome from WSL2 Codex"
  default_prompt: "Use $windows-chrome-playwright-mcp to control Windows Chrome from WSL2 through Playwright MCP."

policy:
  allow_implicit_invocation: true
